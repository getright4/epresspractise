<!DOCTYPE html>
<html lang ="en">
    <head>
      <meta charset="UTF-8">
      <meta name ="viewport" content ="width= device-width", >
      <title>Edit A product</title>
    </head>

    <body>
        <h1>Edit An Existing product By filling The Details</h1>
          <form action="/products/<%= product._id%>?_method=PUT" method="post">
             <label for="name">product Name</label>
             <input type="text" name="name" id="name" placeholder="Enter Name Of Product" value ="<%=product.name%>">
             <label for="price">Product Price(per unit)</label>
             <input type="number" name="price" id="price" placeholder="Enter the Price" value="<%=product.price%>">
             <label for="category">Product Category</label>
             <select name="category" id="category">
                  <%=for(category of categories){ %>
                      <option value="<%=category%>"  <%=product.category === category ? 'selected': ''%>><%=category%></option>
                 <%}%>
             </select>
             <button>Submit</button>
          </form> 

        <a href="/products/<%=products_.id%>">cancel</a>
    </body>
</html>